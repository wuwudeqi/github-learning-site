验收对应表 v1
执行命令和真实结果见 code/README.txt 与 code/output.txt。

RPT-001 -> lab.test.ts 01/02/06
手算部门和总额；精度跨过 Number 安全整数范围；交换明细顺序。

RPT-002 -> 03/04/05
非法金额、重复业务 ID、引号 CSV、错误表头、空输入、受限部门名。

RUN-001 -> 07/08/09
相同重投同任务且只有一条创建事件；同键内容变化冲突；员工/入口/会话隔离。

CAP-001 -> 10/11，ART-001 副本 -> 20
更新目录旧任务保持原标签、新消息取新标签；返回视图不能修改内部状态。

RUN-002 -> 12/13
已收到seq1，完成后读取seq2和3；最大游标返回空；错误游标拒绝。

RUN-003 -> 14/15/16/17/18/19
开始前取消不执行；取消后迟到成功无产物；完成后取消无效；迟到错误不改终态；普通失败；禁止重复启动。

ART-001 -> 20 + demo文件对独立预期的外部检查
当前20只检查输入指纹格式、视图副本以及已存结果指纹不受外部修改影响。
真实demo输出另由验证脚本逐项核对完整Report和CSV文本，结果记录在output.txt。

通过的解释
20条行为测试、独立静态类型检查、演示产物核对，是不同检查，不能折算成模型正确率。
没有调用真实大模型，所以不能报告模型工具选择率或多轮任务成功率。
没有启动OpenCode fork、Web、IM或远程MCP，所以不能报告这些系统端到端联调通过。
没有数据库或多Worker，所以不能报告进程恢复、跨实例幂等或生产并发能力。

真实集成时追加
模型协议录制样例及真实请求；工具schema/业务验证；身份与数据权限；远程重投和断线；Worker退出后的恢复；产物发布故障注入。
每一项写清环境版本、数据、期望、实际结果、失败原因，不用教程通过代替公司验收。
