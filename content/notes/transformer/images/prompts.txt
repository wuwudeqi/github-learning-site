Built-in image_gen used for 01-context-computation.png.
Initial prompt:
Use case: scientific-educational
Asset type: graduate-level Chinese language modeling lecture figure, high-resolution landscape technical infographic, 2560x1600 approximate.
Primary request: Replace a simplistic weather/drink analogy with an academically precise two-context computational comparison. Clean vector-like scientific textbook artwork, pale background, dark ink, restrained blue and orange. No people, cups, cartoons, brains, decorative machines.
Title exact: "上下文如何改变下一 token 的条件分布"
Subtitle exact: "同一组参数 θ，不同上下文 C"
Use three horizontal zones:
TOP: Left two input text rows C₁: “天气很热，我想喝一杯” and C₂: “天气很冷，我想喝一杯”. Both enter ONE shared block labeled "token IDs → Embedding → Causal Transformer". The block produces two last-position row vectors, h₁ and h₂. Arrows flow left to right. Annotate shapes "T × d → 1 × d". Label "上下文改变表示；参数保持不变".
MIDDLE: A rigorously specified tiny OUTPUT-HEAD ONLY worked example. Label "仅演示输出层：d=2，V=3，b=0". Show h₁=[1,0], h₂=[0,1]. A SINGLE shared matrix W with 2 rows and 3 columns: first row "ln 6, ln 3, 0", second row "0, ln 8, 0". Formula z=hW. Output first row z₁=[ln 6,ln 3,0] → softmax → p₁=[0.6,0.3,0.1]. Second row z₂=[0,ln 8,0] → softmax → p₂=[0.1,0.8,0.1]. Show two compact three-bar plots aligned to the candidate labels "冰水 / 热茶 / 果汁"; first bars 60%,30%,10%; second 10%,80%,10%. Make bar LENGTHS proportional to values, shared 0-100% scale.
BOTTOM: Three linked knowledge boxes, short exact labels: "表示：因果注意力汇入前文", "训练：最小化目标 token 的负对数概率", "生成：选出 token，接回前缀". Include one feedback arrow from selected token toward input C, labeled "C → (C,y)".
Footer exact: "教学设定：h 与 W 为手工指定，未模拟完整 Transformer；饮料名假设为单个 token。"
Critical constraints: mathematically exact numbers, matrix dimensions consistent with row-vector convention; no invented attention heatmap; no implication these probabilities are real measurements; hierarchy and generous spacing, text should remain readable when opened full-size. Do not add unrequested equations.

Accuracy revision:
Revise this technical figure for mathematical accuracy. Preserve all correct top context pipeline and middle h/W/z/p formulas exactly. Replace the entire rightmost middle chart area with a simple table, no charts or axes: columns 冰水 热茶 果汁; row p₁ 0.6 0.3 0.1; row p₂ 0.1 0.8 0.1. Replace the entire bottom zone with THREE INDEPENDENT knowledge-reference cards, no arrows anywhere between these cards, no feedback loop: card one heading “表示学习” and text “因果注意力汇入前文”; card two heading “训练目标” and text “负对数似然通过梯度更新 θ”; card three heading “自回归生成” and text “固定 θ，将选出的 token 接回前缀”. Add small bottom-zone heading “关联知识：三个不同问题”. Training is NOT a step in an inference loop. Keep existing footer exactly. Ensure all text stays readable and no cropping.

Other six diagrams are exact SVG generated by code/draw_lesson01.py; numerical validation uses code/lesson01_math.py.
