# 项目执行约定

处理“前沿”日报的生成、补跑、定时触发或恢复时，先读 `docs/frontiers-runbook.md` 和 `.frontiers/current.json`（若存在），再读 `docs/frontiers-editorial.md`。

当前日期尚未线上核验成功时，继续生成和发布；确认定时设置存在不代表本次日报完成。上下文压缩后先恢复进度文件里的日期、阶段和下一步，不能改为回答历史对话中的定时设置问题。

只在用户当前任务与前沿有关时应用上述恢复规则，不影响其他学习笔记或网站任务。
